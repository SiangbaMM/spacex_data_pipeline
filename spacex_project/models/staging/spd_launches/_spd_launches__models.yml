version: 2

models:
  - name: stg_spd_launches__launches
    description: "Launches staging model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - not_null
          - unique
